# Flight Prediction Algorithm Comparison

This project compares ML models (e.g., Random Forest vs. Linear regression ) for predicting flight prices/delays using a Kaggle dataset.

## Setup
- Install: `pip install pandas scikit-learn`
- Run: `python flight_prediction.py`

## Models Compared
- Logistic Regression
- Random Forest

Check `flight_prediction.py` for the code.
